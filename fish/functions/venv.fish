function venv --wraps=python\ -m\ venv\ venv\ /\nsource\ venv/bin/activate.fish --description alias\ venv\ python\ -m\ venv\ venv\ /\nsource\ venv/bin/activate.fish
    python -m venv venv /
    source venv/bin/activate.fish

end
